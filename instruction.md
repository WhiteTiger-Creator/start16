Act as the training-platform engineer picking up a large run that has been down since a failed storage migration. The migration truncated the authoritative checkpoint registry at `/app/data/checkpoint_registry.json` and left the resume planner at `/app/workflow/plan_resume.go` choosing a restart point under draft rules the training governance board later reversed. Nothing the planner proposes can be trusted until that registry is rebuilt. A pre-migration snapshot and a journal of the changes the migrator posted after it survive beside the truncated file under `/app/data`. How the two combine, what an invalidation does, what a later reinstatement brings back and the order of the result are governance decisions, and the registry must be restored at its expected path first.

Then restore the planner, a Go program built with the toolchain installed here and staying a single `package main`. Preserve its `--input` and `--output-dir` options and their defaults; the shard catalogue, the data incident record and the resume policy are always read from their fixed absolute paths under `/app/data`, and `--input` selects the registry only. `/app/docs/resume_contract.json` is the output contract: paths, schemas, required-field lists, container shapes and sort orders. It says nothing about how any value is derived. Reconstruct that from `/app/incident/training_governance_log.md`, mostly routine run notes, where the later dated decision governs. A run writes exactly `/app/output/summary.json`, `/app/output/resume_plan.json` and `/app/output/refetch_queue.jsonl` inside the contract's 90-second budget. Derive every value from the operational inputs: correct against an alternate registry, identical across reruns, and leave the frozen snapshot `/app/workflow/.plan_resume.original.go` exactly as it is.
