A failed storage migration truncated the authoritative checkpoint registry at `/app/data/checkpoint_registry.json` and left the resume planner at `/app/workflow/plan_resume.go` choosing a restart point under draft rules the training governance board later reversed. Nothing the planner proposes can be trusted until that registry is rebuilt. The pre-migration snapshot at `/app/data/registry_snapshot_pre_migration.json` and the journal of the changes the migrator posted after it at `/app/data/registry_journal.json` survive beside the truncated file. The training governance board settled how the two combine, what an invalidation does, what a later reinstatement brings back and what order the result comes out in, and its decisions are in the run log at `/app/incident/training_governance_log.md`. Rebuild the registry in place at `/app/data/checkpoint_registry.json` first, replacing what is there rather than adding to it; whatever you rebuild it with is yours to keep or throw away, since only the registry it writes is read.

Then restore the planner in place at `/app/workflow/plan_resume.go`, a Go program built with the toolchain installed here and staying a single `package main` in that one file. Preserve its `--input` and `--output-dir` options and their defaults; the shard catalogue, the data incident record and the resume policy are always read from their fixed absolute paths under `/app/data`, and `--input` selects the registry only. `/app/docs/resume_contract.json` is the output contract: paths, schemas, required-field lists, container shapes and sort orders. It says nothing about how any value is derived. Reconstruct that from the same run log, which is mostly routine run notes, and where the later dated decision governs. A run writes exactly `/app/output/summary.json`, `/app/output/resume_plan.json` and `/app/output/refetch_queue.jsonl` inside the contract's 90-second budget, and the directory it writes into carries exactly those three afterwards: anything an earlier run left there is cleared, contents only, since the run does not own the path. That end state is a precondition of the run rather than a best effort: where something in the directory cannot be cleared, the run reports the failure and exits non-zero WITHOUT writing its artifacts, since three files standing beside content the run could not remove is not the output the contract names, and a run that returned success over such a directory would be reporting an end state it did not reach. Derive every value from the operational inputs: correct against an alternate registry, identical across reruns, and leave the frozen snapshot `/app/workflow/.plan_resume.original.go` exactly as it is. The snapshot, the journal, the shard catalogue, the data incident record, the resume policy, the run log and `/app/docs/resume_contract.json` are read and not rewritten, and come back byte for byte unchanged; the registry is the one file under `/app/data` that gets replaced.
